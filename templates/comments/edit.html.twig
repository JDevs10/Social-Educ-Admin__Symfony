{% extends 'base.html.twig' %}

{% block body %}
    <h1>Hello from comment edit with id: {{id}}</h1>
    <br>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}